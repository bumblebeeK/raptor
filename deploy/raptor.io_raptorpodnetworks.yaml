---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: raptorpodnetworks.raptor.io
spec:
  group: raptor.io
  names:
    categories:
      - rpn
    kind: RaptorPodNetwork
    listKind: RaptorPodNetworkList
    plural: raptorpodnetworks
    shortNames:
      - rpn
    singular: raptorpodnetwork
  scope: Cluster
  versions:
    - name: v1beta1
      schema:
        openAPIV3Schema:
          description: |-
            RaptorPodNetwork defines an IP pool that can be used for pooled IPAM (i.e. the multi-pool IPAM
            mode).
          properties:
            apiVersion:
              description: |-
                APIVersion defines the versioned schema of this representation of an object.
                Servers should convert recognized schemas to the latest internal value, and
                may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: |-
                Kind is a string value representing the REST resource this object represents.
                Servers may infer this from the endpoint the client submits requests to.
                Cannot be updated.
                In CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              properties:
                cidr:
                  type: string
                  x-kubernetes-validations:
                    - rule: (self == oldSelf)
                is-cluster-instance-subnet:
                  type: boolean
                  x-kubernetes-validations:
                    - rule: (self == oldSelf)
                max-allocate:
                  type: integer
                min-allocate:
                  type: integer
                pre-allocate:
                  type: integer
                selected-namespaces:
                  items:
                    type: string
                  type: array
                selected-node-label:
                  items:
                    type: string
                  type: array
                selected-nodes:
                  items:
                    type: string
                  type: array
                subnet-id:
                  type: string
                  x-kubernetes-validations:
                    - rule: (self == oldSelf)
                trunk-mode:
                  type: boolean
                watermark:
                  type: integer
              type: object
            status:
              description: PodNetworkStatus describe the status of the nodes which uses
                the pool
              properties:
                active:
                  type: boolean
                allocated-count:
                  format: int64
                  type: integer
                allocation-count:
                  format: int64
                  type: integer
                gateway-ip:
                  properties:
                    IPv4:
                      type: string
                    IPv6:
                      type: string
                  required:
                    - IPv4
                    - IPv6
                  type: object
                network-id:
                  type: string
                node-conditions:
                  description: Current service state
                  items:
                    properties:
                      available:
                        format: int64
                        minimum: 0
                        type: integer
                      lastTransitionTime:
                        format: date-time
                        type: string
                      networkCards:
                        items:
                          type: string
                        type: array
                      node:
                        maxLength: 316
                        pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                        type: string
                      total:
                        format: int64
                        minimum: 0
                        type: integer
                      used:
                        format: int64
                        minimum: 0
                        type: integer
                    required:
                      - lastTransitionTime
                      - node
                    type: object
                  type: array
                  x-kubernetes-list-map-keys:
                    - node
                  x-kubernetes-list-type: map
              type: object
          required:
            - spec
          type: object
      served: true
      storage: true
      subresources:
        status: {}
